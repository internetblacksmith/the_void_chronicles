# Void Reader Environment Variables Reference
# 
# ⚠️  DO NOT USE .env FILES - Use Doppler for secrets management
# This file is for reference only to show what variables are needed
# 
# Setup Doppler: https://doppler.com
# For local development:
#   doppler setup --project void-reader --config dev (if you create a dev config)
#   OR just run without secrets (uses defaults)
# For production:
#   doppler setup --project void-reader --config prd

# HTTP Server Configuration
HTTP_PORT=8080

# HTTPS Server Configuration
HTTPS_PORT=8443
# TLS certificates are stored in /data/ssl/ (production) or .ssh/ (development)
# Paths are hardcoded in the application, not configurable via environment

# SSH Server Configuration  
SSH_PORT=2222
SSH_HOST=0.0.0.0
SSH_PASSWORD=Amigos4Life!
SSH_REQUIRE_PASSWORD=true  # Set to "false" to disable password auth (allows any public key)

# Optional: Debug mode
# DEBUG=false

# Monitoring Configuration (MANDATORY in production, FORBIDDEN in development)
# Sentry for error tracking
# SENTRY_DSN=https://your-sentry-dsn@sentry.io/project-id
# SENTRY_ENVIRONMENT=production

# PostHog for analytics
# POSTHOG_API_KEY=phc_your_posthog_api_key
# POSTHOG_HOST=https://app.posthog.com